import type { QuestionDetectionConfig } from './types';
/**
 * Check if a comment contains a question
 */
export declare function isQuestionComment(commentBody: string, config: QuestionDetectionConfig): boolean;
/**
 * Check if a comment was generated by AI (has ai-review-id marker)
 */
export declare function isAiComment(commentBody: string): boolean;
/**
 * Extract the ai-review-id from a comment
 */
export declare function extractAiReviewId(commentBody: string): string | null;
/**
 * Check if a comment is an AI-generated reply
 */
export declare function isAiReply(commentBody: string): boolean;
/**
 * Check if the comment author is a bot
 */
export declare function isBotComment(authorType: string | undefined): boolean;
/**
 * Check if we should trigger a reply
 * Conditions:
 * 1. Comment is not from a bot
 * 2. Comment is not an AI reply itself
 * 3. Comment is a question (if detection enabled)
 */
export declare function shouldTriggerReply(commentBody: string, authorType: string | undefined, questionDetectionConfig: QuestionDetectionConfig): boolean;
/**
 * Create default question detection config
 */
export declare function createDefaultQuestionDetectionConfig(enabled?: boolean): QuestionDetectionConfig;
